# 기능정의서 v1.1

## 1. 문서 목적
이 문서는 업로드된 단일 HTML 앱 `PCB_Count.html`을 기준으로 현재 기능을 역분석하고, 확인된 제약사항에 대한 보완 요구사항까지 반영하여 차기 버전 개발 기준으로 사용할 수 있도록 정리한 개정 기능정의서다.

---

## 2. 앱 개요
- **앱명**: PCB Panel Yield Visualizer
- **문서 버전**: v1.1
- **목적**: 원판(Panel) 규격 안에 PCB 제품을 몇 개 배치할 수 있는지 계산하고, 결과를 시각적으로 보여주며, 치수 조정 시 수량 변화 가능성을 빠르게 탐색하도록 돕는 웹 도구
- **실행 방식**: 브라우저에서 단일 HTML 파일 실행
- **주요 사용자**: PCB 패널 설계 담당자, 생산기술 담당자, 견적 담당자, 영업 엔지니어
- **문서 성격**: 현행 코드 분석 + 차기 개발 반영 요구사항 포함

---

## 3. 핵심 사용자 시나리오
1. 사용자가 원판 규격, 제품 크기, 외곽 로스, 제품 간격, 회전 옵션을 입력한다.
2. 시스템이 실시간으로 패널 내 배치 가능 수량을 계산한다.
3. 시스템이 총 수량, 수율, 배열 수, 남는 공간을 요약 표시한다.
4. 시스템이 캔버스에 실제 배치 상태를 시각적으로 렌더링한다.
5. 시스템이 가로/세로 치수를 조금 조정했을 때 수량 증가 가능 지점을 가이드한다.
6. 사용자가 확대/축소, 드래그 이동, PNG 저장을 수행한다.
7. 차기 버전에서는 저장, 비교, 혼합 배치, 검증 메시지, 수동 배치 기능까지 확장한다.

---

## 4. 기능 목록

| 기능ID | 기능명 | 상세설명 | 입력 | 출력 | 우선순위 |
|---|---|---|---|---|---|
| F-01 | 원판 규격 선택 | 프리셋(1000x1000, 1200x1000) 또는 사용자 직접 입력 방식으로 원판 크기를 설정한다. | panelPreset, panelW, panelH | 내부 계산용 원판 W/H | 높음 |
| F-02 | 제품 규격 입력 | PCB 제품의 가로/세로 치수를 입력받는다. 최소값 제한이 적용된다. | partW, partH | 내부 계산용 제품 W/H | 높음 |
| F-03 | 공정 여유 입력 | 외곽 로스와 제품 간 간격(GAP)을 입력받아 배치 가능 영역을 계산한다. | borderLoss, gap | 유효 작업영역, 배치 간격 | 높음 |
| F-04 | 회전 옵션 선택 | 정방향 고정, 90도 회전 고정, 자동 최적 수량 모드 중 하나를 선택한다. | rotateMode | 배치 방향 결정 | 높음 |
| F-05 | 수량 계산 | 유효 작업영역 기준으로 행/열 개수를 계산하고 총 PCB 수량을 산출한다. | panel, part, loss, gap, rotate | cols, rows, count | 높음 |
| F-06 | 수율 계산 | 전체 원판 면적 대비 실제 PCB 면적 비율을 백분율로 계산한다. | count, partW, partH, panelW, panelH | efficiency(%) | 높음 |
| F-07 | 남는 공간 계산 | 배치 후 남은 가로/세로 공간을 계산한다. | usedW, usedH, uw, uh | remaining W/H | 중간 |
| F-08 | 분석 결과 요약 표시 | 총 수량, 원판 수율, 배열(열×행), 남은 공간을 카드 UI로 보여준다. | 계산 결과 | resultBox UI | 높음 |
| F-09 | 캔버스 배치 시각화 | 원판, 외곽 로스, PCB 배치, GAP, 자투리 영역을 캔버스에 렌더링한다. | 계산 결과, zoom | canvas 시각화 | 높음 |
| F-10 | 치수선 표시 | 원판 크기, 외곽 로스, 제품 크기, GAP, 남는 공간을 보조 치수선으로 표시한다. | scale, layout data | dimension overlay | 중간 |
| F-11 | 최적화 사이즈 가이드 | 현재 제품 크기 기준으로, 가로/세로를 줄였을 때 1줄 추가 가능한 지점과 늘렸을 때 수량 감소 직전 한계를 계산한다. | current W/H, current count | targetWAdd, targetWLimit, targetHAdd, targetHLimit | 높음 |
| F-12 | 확대/축소 | 슬라이더, 마우스 휠, 초기화 버튼으로 확대/축소를 제어한다. | zoomRange, mouse wheel, resetZoom | currentZoom, canvas rescale | 중간 |
| F-13 | 캔버스 이동 | 드래그로 큰 도면 영역을 패닝 이동한다. | mouse drag | scroll position update | 중간 |
| F-14 | PNG 저장 | 현재 캔버스 상태를 PNG 이미지로 저장한다. | downloadBtn click | PNG 파일 다운로드 | 중간 |
| F-15 | 반응형 재렌더링 | 입력값 변경 또는 창 크기 변경 시 전체 결과를 다시 계산하고 다시 그린다. | input/change/resize event | 최신 결과와 화면 | 높음 |
| F-16 | 입력값 자동 저장/복원 | 최근 입력 조건을 브라우저에 자동 저장하고 재방문 시 복원한다. | panel, part, gap, loss, rotate 등 | 마지막 작업조건 자동 복원 | 높음 |
| F-17 | 사용자 프리셋 저장/불러오기 | 자주 쓰는 조건 세트를 이름으로 저장하고 다시 불러올 수 있다. | preset name, input values | 저장된 사용자 프리셋 목록 | 높음 |
| F-18 | 계산 이력 조회 | 최근 계산한 조건과 결과를 목록으로 확인한다. | 실행 시점 계산값 | 최근 계산 이력 리스트 | 중간 |
| F-19 | 다중 제품 등록 | 두 개 이상의 제품 크기를 등록할 수 있다. | product list | 제품 목록 데이터 | 중간 |
| F-20 | 혼합 배치 비교/시뮬레이션 | 단일 제품뿐 아니라 복수 제품 기준 비교 또는 혼합 배치 가능성을 탐색한다. | product list, rule | 비교 결과 또는 혼합안 | 중간 |
| F-21 | 공정 제약 옵션 입력 | 툴링홀, 가공 여백, V-cut, 비대칭 금지구역 등 실제 공정 조건을 입력받는다. | process options | 실공정 반영 계산조건 | 높음 |
| F-22 | 이중 수율 표시 | 단순 면적 수율과 실공정 반영 수율을 구분하여 함께 보여준다. | area data, process constraints | dual yield result | 높음 |
| F-23 | 입력 검증 및 에러 메시지 | 비정상 입력값에 대해 사용자 친화적 메시지를 표시한다. | invalid values | validation / error UI | 높음 |
| F-24 | 계산 불가 상태 안내 | 결과 산출이 불가능한 경우 안내 상태를 표시한다. | invalid / impossible state | empty state / warning UI | 중간 |
| F-25 | 자동/수동 배치 모드 전환 | 자동 배치와 수동 배치 시뮬레이션 모드를 UI에서 전환한다. | mode toggle | mode state | 중간 |
| F-26 | 수동 배치 미리보기 | 마우스 위치 기준으로 제품 배치 가능 여부를 시각적으로 표시한다. | mouse position | placement preview | 중간 |
| F-27 | 수동 배치 고정안 저장 | 사용자가 확인한 수동 배치안을 저장하거나 별도 결과로 유지한다. | manual placement data | saved manual layout | 낮음 |
| F-28 | 단위 전환 | mm / inch 단위를 전환하고 자동 환산한다. | unit toggle | converted values | 낮음 |
| F-29 | 조건 비교 기능 | 조건 A/B 또는 회전 방식별 결과를 비교한다. | compare set | comparison table | 중간 |
| F-30 | 데이터 리포트 내보내기 | PNG 외에 CSV/Excel/리포트 형식으로 계산 결과를 저장한다. | export action | data file/report | 중간 |
| F-31 | 공유 기능 | 설정값 JSON 저장/불러오기 또는 URL 기반 공유를 지원한다. | current config | sharable config | 낮음 |
| F-32 | 모바일 대응 UI | 태블릿/모바일에서 사용할 수 있도록 레이아웃을 최적화한다. | viewport | responsive layout | 중간 |

---

## 5. 기능 상세 정의

### F-01. 원판 규격 선택
- 사용자는 드롭다운에서 아래 옵션을 선택할 수 있다.
  - 1000 x 1000 (레귤러)
  - 1200 x 1000 (점보)
  - 직접 입력
- 직접 입력 선택 시 원판 W/H 입력 필드가 표시된다.
- 프리셋 선택 시 해당 값이 자동으로 원판 W/H에 반영된다.

### F-02. 제품 규격 입력
- 제품 가로(W), 세로(H)를 수치로 입력한다.
- 최소 허용값은 내부 계산 기준으로 보정된다.
  - 원판 W/H 최소 100
  - 제품 W/H 최소 10
- 차기 버전에서는 잘못된 값 입력 시 즉시 검증 메시지를 제공해야 한다.

### F-03. 공정 여유 입력
- 외곽 로스는 원판 가장자리 사용 불가 구간을 의미한다.
- 제품 간격은 PCB 간 이격 거리다.
- 시스템은 `유효 작업영역 = 원판 크기 - 외곽 로스 양쪽 합`으로 계산한다.
- 차기 버전에서는 비대칭 로스, 작업 금지구역, 가공 영역도 별도 지정 가능해야 한다.

### F-04. 회전 옵션 선택
- **자동 최적 수량**: 정방향/90도 회전 두 경우를 모두 계산하여 더 많은 수량을 선택
- **정방향 고정**: 입력된 W/H 그대로 배치
- **90도 회전 고정**: W/H를 바꿔서 배치
- 차기 버전에서는 회전 옵션별 결과를 비교 표로 제공할 수 있어야 한다.

### F-05. 수량 계산
- 배치 열 수와 행 수는 아래 개념으로 계산된다.
  - `cols = floor((유효가로 + gap) / (제품가로 + gap))`
  - `rows = floor((유효세로 + gap) / (제품세로 + gap))`
- 총 수량은 `cols * rows`
- 회전 자동 모드에서는 정방향 결과와 90도 회전 결과를 비교해 더 큰 값을 채택한다.
- 차기 버전에서는 다중 제품과 혼합 배치 계산 규칙도 확장 반영해야 한다.

### F-06. 수율 계산
- 현행 수율은 전체 원판 면적 대비 PCB 실면적 총합 비율이다.
- 계산식 개념:
  - `수율 = (총 PCB 면적 / 원판 전체 면적) * 100`
- 소수점 첫째 자리까지 표시한다.
- 차기 버전에서는 아래 두 값을 분리해야 한다.
  - 단순 면적 수율
  - 실공정 제약 반영 수율

### F-07. 남는 공간 계산
- 배치에 사용된 실제 폭/높이를 계산한다.
- 남는 가로/세로 공간을 각각 도출한다.
- 결과는 요약 카드와 캔버스 치수선에 반영된다.

### F-08. 분석 결과 요약 표시
- 아래 항목을 결과 카드로 표시한다.
  - 총 수량 (PCS)
  - 원판 수율 (%)
  - 배열 (열 × 행)
  - 남은 가로/세로(mm)
- 차기 버전에서는 아래 정보도 추가 가능해야 한다.
  - 실공정 반영 수율
  - 선택한 공정 옵션 요약
  - 비교 대상 결과 차이

### F-09. 캔버스 배치 시각화
- 캔버스에는 아래 요소가 렌더링된다.
  - 원판 배경
  - 외곽 로스 금지구역
  - PCB 배치 블록
  - 치수선
  - 잔여 공간 표시
- 색상 의미는 범례와 일치한다.
- 차기 버전에서는 수동 배치 미리보기와 공정 금지구역도 함께 시각화해야 한다.

### F-10. 치수선 표시
- 원판 전체 크기 표시
- 외곽 로스 두께 표시
- 첫 번째 PCB 블록의 폭/높이 표시
- GAP 표시
- 남는 자투리 공간 표시
- 치수선은 확대/축소 배율과 함께 다시 계산되어 렌더링된다.

### F-11. 최적화 사이즈 가이드
- 현재 제품 W/H를 기준으로 ±1mm 단위로 최대 100mm 범위를 탐색한다.
- 가로/세로 각각에 대해 아래 정보를 제공한다.
  - **1줄 추가 가능 지점**: 치수를 줄였을 때 현재보다 더 많은 수량이 들어가는 최초 지점
  - **수량 한계 지점**: 치수를 늘렸을 때 현재 수량이 유지되는 최대 지점
- 값이 없으면 `범위 외`로 표시한다.
- 차기 버전에서는 제품별 비교 및 조건별 가이드 비교도 가능해야 한다.

### F-12. 확대/축소
- 슬라이더 범위: 50% ~ 300%
- 마우스 휠 확대/축소 지원
- 리셋 버튼으로 100% 복귀
- 줌 변경 시 현재 보고 있는 위치를 최대한 유지하도록 스크롤 위치를 재계산한다.

### F-13. 캔버스 이동
- 캔버스 영역이 큰 경우, 마우스 드래그로 좌우/상하 이동 가능하다.
- 좌클릭 드래그만 허용된다.

### F-14. PNG 저장
- 사용자가 `이미지 저장 (PNG)` 버튼을 클릭하면 현재 캔버스 이미지를 PNG로 다운로드한다.
- 파일명은 `PCB_Layout.png`
- 차기 버전에서는 이미지 저장 외 수치 데이터 내보내기도 지원해야 한다.

### F-15. 반응형 재렌더링
- 아래 이벤트 발생 시 자동 재계산 및 재렌더링 된다.
  - 입력값 변경
  - 프리셋 변경
  - 회전 옵션 변경
  - 줌 변경
  - 브라우저 리사이즈

### F-16. 입력값 자동 저장/복원
- 사용자가 입력한 마지막 계산 조건은 브라우저 로컬 저장소에 자동 저장되어야 한다.
- 새로고침 또는 재방문 시 마지막 사용값을 자동 복원해야 한다.
- 사용자는 초기화 버튼으로 저장된 값을 삭제할 수 있어야 한다.

### F-17. 사용자 프리셋 저장/불러오기
- 사용자는 특정 설정 조합을 이름과 함께 저장할 수 있어야 한다.
- 저장 가능한 항목 예시:
  - 원판 규격
  - 제품 크기
  - 외곽 로스
  - GAP
  - 회전 옵션
  - 공정 옵션
- 저장된 프리셋은 목록에서 선택해 즉시 불러올 수 있어야 한다.

### F-18. 계산 이력 조회
- 최근 수행한 계산 결과를 시간순으로 보관한다.
- 이력에는 최소 아래 항목이 포함되어야 한다.
  - 계산 시각
  - 핵심 입력값
  - 총 수량
  - 수율
- 사용자는 이력 중 하나를 다시 불러와 재계산할 수 있어야 한다.

### F-19. 다중 제품 등록
- 사용자는 제품 2종 이상을 등록할 수 있어야 한다.
- 제품마다 아래 속성을 입력할 수 있어야 한다.
  - 제품명
  - 가로/세로
  - 우선순위
  - 목표 수량 또는 고정 수량

### F-20. 혼합 배치 비교/시뮬레이션
- 시스템은 단일 제품 모드와 복수 제품 비교 모드를 구분해야 한다.
- 최소 단계에서는 아래 두 가지 중 하나를 우선 제공한다.
  - 제품별 단독 계산 비교
  - 간단한 혼합 배치 시뮬레이션
- 향후에는 자동 최적화 알고리즘을 통해 조합 최적화를 검토할 수 있다.

### F-21. 공정 제약 옵션 입력
- 사용자는 실제 제조 제약을 반영하기 위한 공정 옵션을 설정할 수 있어야 한다.
- 예시 옵션:
  - 상/하/좌/우 비대칭 여백
  - 툴링홀 영역
  - 가이드홀 영역
  - V-cut / routing 마진
  - 설비별 최소 이격 조건
  - 작업 금지구역

### F-22. 이중 수율 표시
- 시스템은 아래 두 수율을 함께 제공해야 한다.
  - 단순 면적 수율
  - 실공정 반영 수율
- 두 수율의 차이를 한눈에 볼 수 있도록 표 또는 카드로 구분 표시해야 한다.

### F-23. 입력 검증 및 에러 메시지
- 비어 있는 입력값, 음수, 허용 범위 초과, 계산 불가 조합 등에 대해 사용자 친화적 메시지를 제공해야 한다.
- 메시지는 입력 필드 근처 또는 결과 영역 상단에 표시한다.
- 자동 보정이 발생하면 보정 사실을 사용자에게 안내해야 한다.

### F-24. 계산 불가 상태 안내
- 아래와 같은 경우 결과 카드 대신 안내 상태를 표시해야 한다.
  - 제품이 원판보다 큰 경우
  - 유효 작업영역이 0 이하가 되는 경우
  - 공정 옵션 적용 후 배치 불가인 경우
- 안내 문구는 원인과 해결 힌트를 함께 제공해야 한다.

### F-25. 자동/수동 배치 모드 전환
- 사용자는 토글 UI를 통해 자동 배치와 수동 배치를 선택할 수 있어야 한다.
- 자동 배치는 기존 계산 로직을 기반으로 한다.
- 수동 배치는 사용자의 포인터 위치를 이용한 시뮬레이션 기반으로 한다.

### F-26. 수동 배치 미리보기
- 수동 모드에서는 마우스 위치 기준으로 제품이 배치 가능하면 허용 색상, 불가능하면 경고 색상으로 표시해야 한다.
- 현재 좌표와 경계 여유값을 함께 안내할 수 있어야 한다.

### F-27. 수동 배치 고정안 저장
- 사용자가 검토한 수동 배치 상태를 하나의 배치안으로 저장할 수 있어야 한다.
- 저장된 배치안은 다시 불러오거나 PNG/리포트로 내보낼 수 있어야 한다.

### F-28. 단위 전환
- 사용자는 mm / inch 단위를 선택할 수 있어야 한다.
- 내부 계산 기준 단위는 통일하되, 입력/표시는 선택 단위로 자동 환산해야 한다.

### F-29. 조건 비교 기능
- 사용자는 조건 A와 조건 B를 나란히 비교할 수 있어야 한다.
- 비교 항목 예시:
  - 총 수량
  - 수율
  - 남는 공간
  - 회전 여부
  - 공정 옵션 반영 결과

### F-30. 데이터 리포트 내보내기
- 시스템은 PNG 외에 아래 형식의 저장을 지원해야 한다.
  - CSV
  - Excel
  - 계산 리포트(PDF 또는 인쇄용 화면)
- 리포트에는 입력 조건, 계산 결과, 시각화 이미지가 함께 포함될 수 있어야 한다.

### F-31. 공유 기능
- 사용자는 현재 설정을 저장하거나 다른 사용자와 공유할 수 있어야 한다.
- 최소 지원 항목 예시:
  - 설정값 JSON 저장/불러오기
  - URL 파라미터 공유
  - 캡처 + 수치 요약 리포트 공유

### F-32. 모바일 대응 UI
- 태블릿/모바일 화면에서도 핵심 기능 사용이 가능해야 한다.
- 작은 화면에서는 아래와 같은 대응이 필요하다.
  - 1열 레이아웃 전환
  - 캔버스 접기/펼치기
  - 결과 카드 상단 고정
  - 주요 입력 섹션 아코디언화

---

## 6. 화면 구성 기준 기능 맵

| 화면영역 | 포함 기능 |
|---|---|
| 헤더 | 앱 제목, PNG 저장, 향후 리포트/공유 진입 |
| 좌측 사이드바 - 규격 설정 | 원판 프리셋, 사용자 규격 입력, 제품 크기, 로스, GAP, 회전, 줌, 단위 |
| 좌측 사이드바 - 분석 결과 | 총 수량, 수율, 배열, 남은 공간, 실공정 수율 |
| 좌측 사이드바 - 최적화 가이드 | W/H 조정 시 수량 증가 및 한계 지점 |
| 좌측 사이드바 - 검증/안내 영역 | 입력 오류, 자동 보정, 계산 불가 안내 |
| 좌측 사이드바 - 프리셋/이력 | 사용자 저장 조건, 최근 계산 이력 |
| 좌측 사이드바 - 범례 | 색상 의미 설명 |
| 우측 메인 캔버스 | 배치도 렌더링, 치수선, 확대/축소, 드래그 이동, 수동 배치 미리보기 |

---

## 7. 비기능 요구사항
- **실시간성**: 입력 즉시 결과가 다시 계산되어야 한다.
- **가시성**: 계산값과 시각화가 동시에 업데이트되어야 한다.
- **휴대성**: 기본 프로토타입은 HTML 단일 파일로도 실행 가능해야 한다.
- **사용성**: 엔지니어가 숫자를 바꾸며 빠르게 비교할 수 있어야 한다.
- **확장성**: 다중 제품, 공정 옵션, 비교, 저장 기능을 단계적으로 붙일 수 있어야 한다.
- **내보내기**: 이미지뿐 아니라 수치 데이터 및 보고서 저장이 가능해야 한다.
- **반응형 대응**: 태블릿/모바일 환경에서도 핵심 기능이 유지되어야 한다.

---

## 8. 반영된 개정 요구사항 요약

### 8-1. 저장 기능 반영
- 마지막 입력값 자동 복원
- 사용자 프리셋 저장/불러오기
- 최근 계산 이력 확인

### 8-2. 제품 구성 확장 반영
- 단일 제품 → 다중 제품 등록 구조로 확장
- 혼합 배치 또는 비교 시뮬레이션 가능 구조 반영

### 8-3. 수율 계산 정교화 반영
- 단순 면적 수율 유지
- 실공정 제약 반영 수율 추가
- 공정 옵션 별도 입력 구조 반영

### 8-4. 검증 UX 반영
- 입력 검증 메시지
- 계산 불가 상태 안내
- 자동 보정 안내

### 8-5. 수동 모드 반영
- 자동/수동 배치 토글 UI
- 수동 배치 미리보기
- 수동 배치안 저장 가능성 반영

### 8-6. 추가 확장 요구 반영
- 단위 전환
- 조건 비교
- CSV/Excel/리포트 저장
- 공유 기능
- 모바일 대응

---

## 9. 향후 문서 연계 메모
다음 단계 문서에서는 아래 내용을 이어서 상세화할 수 있다.
- `화면(UI/UX)정의서.md`: 컴포넌트 구조, 사용 흐름, 상태 변화, 반응형/검증 UX
- `개발Stack 제안서.md`: 프론트엔드 단일파일 / 웹앱 / 풀스택 확장 구조 비교
- `프로토타입 프롬프트.md`: AI 코딩 툴에 바로 넣을 수 있는 구현 지시문
