# 화면(UI/UX)정의서 v1

## 1. 문서 목적
이 문서는 `PCB_Count.html`의 실제 HTML/CSS/JS 구조를 기준으로 현재 UI를 역분석하고, 추천 화면 구조를 반영하여 차기 버전의 공식 화면 설계 기준으로 사용하기 위한 문서다. 본 문서는 특히 **화면모드(라이트 모드, 다크 모드, 블루-그레이 모드)**를 최우선 항목으로 정의하며, **라이트 모드를 기본 화면모드**로 지정한다. 기준 앱은 PCB Panel Yield Visualizer이며, 입력 중심 좌측 패널과 시각화 중심 우측 캔버스 구조를 가진 엔지니어링 도구다. fileciteturn0file0

---

## 2. 화면 설계 핵심 원칙

### 2-1. 화면모드 최우선 정책
차기 버전은 아래 3가지 화면모드를 지원해야 한다.

| 모드ID | 모드명 | 설명 | 기본 여부 |
|---|---|---|---|
| TM-01 | 라이트 모드 | 업무 문서형 가독성을 우선한 밝은 기본 화면 | 기본값 |
| TM-02 | 다크 모드 | 저조도 환경과 장시간 작업에 적합한 모드 | 선택 |
| TM-03 | 블루-그레이 모드 | 현행 앱의 엔지니어링 감성을 유지한 전문형 모드 | 선택 |

#### 필수 요구사항
- 최초 진입 시 기본값은 **라이트 모드**여야 한다.
- 사용자는 헤더 우측에서 1클릭 또는 2클릭 이내에 화면모드를 바꿀 수 있어야 한다.
- 마지막 선택 모드는 브라우저 재방문 시 유지되어야 한다.
- 테마는 아래 전 요소에 일괄 적용되어야 한다.
  - 앱 배경
  - 헤더
  - 카드
  - 입력 필드
  - 버튼
  - 결과 카드
  - 범례
  - 캔버스 컨테이너
  - 상태 배지
  - 검증/에러/성공 컬러

### 2-2. 화면 구성 원칙
- 입력은 빠르게, 결과는 즉시, 시각화는 크게 보여야 한다.
- 가장 많이 쓰는 기능은 헤더와 좌측 패널 상단에 둔다.
- 고급 기능은 접을 수 있게 설계해 초보자의 부담을 줄인다.
- 캔버스는 이 앱의 핵심 가치이므로 항상 가장 큰 시각적 비중을 가져야 한다.
- 숫자, 상태, 비교 결과는 한눈에 읽혀야 한다.

### 2-3. UX 설계 원칙
- 엔지니어가 반복 실험하기 쉬운 구조여야 한다.
- 입력값 변경 즉시 결과가 반영되어야 한다.
- 잘못된 값은 바로 알려줘야 한다.
- 추천값은 “보기만 하는 정보”가 아니라 “즉시 적용 가능한 액션”이어야 한다.
- 저장, 비교, 공유로 이어지기 쉬운 흐름이어야 한다.

---

## 3. 코드 기반 현행 UI 구조 해석
현재 HTML은 아래 구조를 가진다. fileciteturn0file0

- 상단 sticky 헤더
- 좌측 세로 스크롤 사이드바
- 우측 대형 캔버스 뷰어
- 좌측 카드 구성
  - 규격 설정
  - 분석 결과
  - 최적화 사이즈 가이드
  - 범례
- 우측은 캔버스 단일 중심 구조

현재 UI는 이미 “입력 → 실시간 계산 → 시각화 확인” 흐름이 잘 잡혀 있다. 다만 차기 버전에서는 아래 보강이 필요하다.
- 화면모드 전환 UI 없음
- 입력 검증/에러 UX 없음
- 결과 상태 구분 없음
- 추천값 즉시 적용 없음
- 캔버스 툴바 없음
- 프리셋/이력/공정옵션 진입 구조 없음
- 모바일 대응 정의 부족

---

## 4. 공식 추천 화면 구조 v1
본 문서는 아래 구조를 차기 버전의 기본 화면 레이아웃으로 정의한다.

### 4-1. 전체 레이아웃
- **상단 헤더**
- **좌측 컨트롤 패널**
- **우측 메인 시각화 영역**
- **상태바(선택 사항이지만 강력 권장)**

레이아웃 기준:
- 데스크톱: 2열 레이아웃
- 태블릿: 축소형 2열 또는 1.5열 레이아웃
- 모바일: 1열 스택형 레이아웃

---

## 5. 헤더 정의

### 5-1. 헤더 구성
| 항목ID | 요소명 | 설명 | 우선순위 |
|---|---|---|---|
| HD-01 | 앱명 | PCB Panel Yield Visualizer | 높음 |
| HD-02 | 화면모드 선택 | 라이트/다크/블루-그레이 전환 | 최우선 |
| HD-03 | 도움말 | 용어 설명, 입력 예시, 사용 가이드 | 중간 |
| HD-04 | PNG 저장 | 현재 캔버스 저장 | 높음 |
| HD-05 | 리포트/내보내기 | CSV, Excel, PDF 등 확장 저장 | 중간 |

### 5-2. 헤더 UX 규칙
- 앱명은 좌측 정렬
- 우측에는 `화면모드 > 도움말 > 저장` 순으로 배치 권장
- 화면모드 선택은 아이콘과 텍스트를 함께 보여준다.
  - ☀ 라이트
  - 🌙 다크
  - 🧊 블루-그레이
- 저장 버튼은 주요 액션으로 강조한다.
- 모바일에서는 도움말과 확장 저장은 메뉴 안으로 접을 수 있다.

---

## 6. 좌측 컨트롤 패널 정의
좌측 패널은 사용자가 입력, 결과 확인, 비교, 프리셋 관리까지 수행하는 핵심 작업 구역이다.

### 6-1. 섹션 구성 순서
1. 규격 설정
2. 고급 공정 옵션
3. 분석 결과
4. 최적화 가이드
5. 최근 이력 / 프리셋
6. 범례

이 순서를 공식 구조로 정의한다.

---

## 7. 규격 설정 섹션 정의

| 항목ID | 요소명 | 설명 | 현재 상태 | v1 반영 |
|---|---|---|---|---|
| CFG-01 | 원판 프리셋 | 레귤러/점보/직접입력 | 있음 | 유지 |
| CFG-02 | 원판 W/H 입력 | 사용자 직접 입력 | 있음 | 유지 |
| CFG-03 | 제품 W/H 입력 | PCB 치수 입력 | 있음 | 유지 |
| CFG-04 | 외곽 로스 입력 | 가장자리 금지구역 | 있음 | 유지 |
| CFG-05 | 제품 GAP 입력 | 제품 간 거리 | 있음 | 유지 |
| CFG-06 | 회전 옵션 | 자동/정방향/90도 | 있음 | 유지 |
| CFG-07 | 단위 전환 | mm / inch | 없음 | 추가 |
| CFG-08 | 줌 컨트롤 | 배율 조절과 초기화 | 있음 | 유지 |
| CFG-09 | 입력 검증 메시지 | 비정상 입력 피드백 | 없음 | 필수 추가 |

### 규격 설정 UX 규칙
- 기본 입력은 항상 접히지 않고 바로 보여야 한다.
- 단위는 입력 필드 우측 또는 섹션 상단에서 전환 가능해야 한다.
- 숫자 입력은 최소/최대/권장값 안내를 줄 수 있어야 한다.
- 검증 메시지는 필드 바로 아래에 보여야 한다.
- 라이트 모드에서도 입력 대비와 포커스 표시가 분명해야 한다.

---

## 8. 고급 공정 옵션 섹션 정의
이 섹션은 초보자에게 부담을 주지 않기 위해 기본 접힘 상태로 제공한다.

| 항목ID | 요소명 | 설명 | v1 반영 |
|---|---|---|---|
| ADV-01 | 비대칭 여백 설정 | 상/하/좌/우 개별 여백 | 추가 |
| ADV-02 | 툴링홀 옵션 | 공정 홀 영역 지정 | 추가 |
| ADV-03 | V-cut / Routing 마진 | 절단/가공 마진 지정 | 추가 |
| ADV-04 | 작업 금지구역 | 배치 제외 영역 지정 | 추가 |
| ADV-05 | 실공정 계산 반영 토글 | 단순/실공정 비교 표시 | 추가 |

### 고급 옵션 UX 규칙
- 기본값은 닫힘 상태
- 사용자가 켜면 요약 배지로 현재 옵션을 보여준다.
- 결과 카드와 연동되어야 한다.

---

## 9. 분석 결과 섹션 정의

| 항목ID | 요소명 | 설명 | 현재 상태 | v1 반영 |
|---|---|---|---|---|
| RES-01 | 총 수량 | 배치 가능 PCS 수 | 있음 | 유지 |
| RES-02 | 단순 수율 | 면적 기준 수율 | 있음 | 유지 |
| RES-03 | 실공정 수율 | 공정 반영 수율 | 없음 | 추가 |
| RES-04 | 배열 정보 | 열 × 행 | 있음 | 유지 |
| RES-05 | 남는 공간 | 가로/세로 잔여 | 있음 | 유지 |
| RES-06 | 상태 배지 | 정상/주의/불가 | 없음 | 추가 |
| RES-07 | 비교 증감 | 이전 조건 대비 변화 | 없음 | 추가 |

### 분석 결과 UX 규칙
- 가장 큰 숫자는 총 수량이어야 한다.
- 수율은 단순 수율과 실공정 수율을 구분해서 보여준다.
- 상태 배지는 색상과 텍스트를 함께 사용한다.
- 입력값이 바뀌면 결과 변화가 눈에 띄어야 한다.

---

## 10. 최적화 가이드 섹션 정의

| 항목ID | 요소명 | 설명 | 현재 상태 | v1 반영 |
|---|---|---|---|---|
| GDE-01 | 가로 조정 분석 | 1줄 추가/한계값 | 있음 | 유지 |
| GDE-02 | 세로 조정 분석 | 1줄 추가/한계값 | 있음 | 유지 |
| GDE-03 | 현재값 태그 | 현재 W/H 표시 | 있음 | 유지 |
| GDE-04 | 추천값 설명 | 왜 추천하는지 설명 | 없음 | 추가 |
| GDE-05 | 즉시 적용 버튼 | 클릭 시 값 반영 | 없음 | 필수 추가 |
| GDE-06 | 증가/감소 수량 표시 | 변화량 가시화 | 없음 | 추가 |

### 최적화 가이드 UX 규칙
- 추천값은 버튼 형태 또는 칩 형태로 제공한다.
- 클릭하면 관련 입력값에 즉시 반영된다.
- `현재보다 -3mm 하면 1열 추가` 같은 문장을 함께 제공한다.

---

## 11. 최근 이력 / 프리셋 섹션 정의

| 항목ID | 요소명 | 설명 | v1 반영 |
|---|---|---|---|
| HIS-01 | 최근 계산 이력 | 최근 조건과 결과 목록 | 추가 |
| HIS-02 | 프리셋 저장 | 설정 조합 저장 | 추가 |
| HIS-03 | 프리셋 불러오기 | 저장값 재적용 | 추가 |
| HIS-04 | 비교 시작 버튼 | 선택 조건 비교 시작 | 추가 |

### 이력/프리셋 UX 규칙
- 이력은 최근순 정렬
- 프리셋은 이름과 핵심 값 요약을 함께 보여준다.
- 이력/프리셋 항목은 클릭 즉시 재적용 가능해야 한다.

---

## 12. 범례 섹션 정의

| 항목ID | 요소명 | 설명 | 현재 상태 | v1 반영 |
|---|---|---|---|---|
| LEG-01 | 원판 설명 | 배경 패널 의미 | 있음 | 유지 |
| LEG-02 | PCB 설명 | 제품 배치 영역 | 있음 | 유지 |
| LEG-03 | 외곽 로스 설명 | 금지구역 | 있음 | 유지 |
| LEG-04 | GAP 설명 | 제품 간격 | 있음 | 유지 |
| LEG-05 | 남는 영역 설명 | 자투리 공간 | 있음 | 유지 |
| LEG-06 | 테마 대응 범례 | 테마별 대비 유지 | 없음 | 추가 |

### 범례 UX 규칙
- 각 테마에서 충분한 색상 대비가 유지되어야 한다.
- 색약 사용자를 위해 텍스트 라벨과 패턴 보조를 고려한다.

---

## 13. 우측 메인 시각화 영역 정의

### 13-1. 구조
우측 메인 영역은 아래 순서를 권장 구조로 정의한다.
1. 캔버스 툴바
2. 메인 배치 캔버스
3. 상태바

### 13-2. 캔버스 툴바 정의
| 항목ID | 요소명 | 설명 | v1 반영 |
|---|---|---|---|
| TB-01 | 전체 보기 | 전체 레이아웃 맞춤 | 추가 |
| TB-02 | 중심 맞춤 | 중앙 기준 정렬 | 추가 |
| TB-03 | 100% 보기 | 원래 배율 복귀 | 추가 |
| TB-04 | 확대/축소 버튼 | 휠 외 버튼 제공 | 추가 |
| TB-05 | 수동 배치 토글 | 자동/수동 모드 전환 | 추가 |

### 13-3. 메인 캔버스 정의
| 항목ID | 요소명 | 설명 | 현재 상태 | v1 반영 |
|---|---|---|---|---|
| CVS-01 | 배치 시각화 | 패널 및 PCB 배열 표시 | 있음 | 유지 |
| CVS-02 | 치수선 표시 | 크기/간격 표시 | 있음 | 유지 |
| CVS-03 | 휠 줌 | 마우스 휠 확대축소 | 있음 | 유지 |
| CVS-04 | 드래그 패닝 | 큰 도면 이동 | 있음 | 유지 |
| CVS-05 | 수동 미리보기 | 마우스 위치 배치 확인 | 내부 로직만 있음 | UI화 |
| CVS-06 | 공정 금지구역 표시 | 실공정 영역 표시 | 없음 | 추가 |
| CVS-07 | 추천 포인트 오버레이 | 남는 공간/추천치 강조 | 없음 | 추가 |

### 13-4. 상태바 정의
| 항목ID | 요소명 | 설명 | v1 반영 |
|---|---|---|---|
| STB-01 | 현재 줌 배율 | 예: 100% | 추가 |
| STB-02 | 현재 좌표 | 마우스 기준 좌표 | 추가 |
| STB-03 | 배치 가능 여부 | 가능/불가 표시 | 추가 |
| STB-04 | 회전 상태 | 현재 적용 회전 표시 | 추가 |

### 시각화 UX 규칙
- 캔버스는 앱에서 가장 큰 비중을 가진다.
- 사용자는 현재 위치와 배치 가능 상태를 쉽게 알아야 한다.
- 라이트 모드에서는 캔버스 외곽과 내부 대비를 충분히 확보해야 한다.
- 다크/블루-그레이 모드에서는 장시간 사용 시 피로를 줄이는 대비가 필요하다.

---

## 14. 사용자 플로우 정의

### 14-1. 기본 계산 플로우
1. 사용자가 기본값으로 진입한다.
2. 라이트 모드가 기본으로 적용된다.
3. 사용자가 규격을 수정한다.
4. 결과 카드와 캔버스가 즉시 갱신된다.
5. 사용자는 가이드에서 추천값을 보고 바로 적용할 수 있다.

### 14-2. 화면모드 전환 플로우
1. 사용자가 헤더의 화면모드 선택을 연다.
2. 라이트/다크/블루-그레이 중 하나를 선택한다.
3. UI 전체 토큰이 즉시 변경된다.
4. 마지막 선택값은 저장된다.

### 14-3. 프리셋/이력 플로우
1. 사용자가 설정값으로 계산한다.
2. 조건을 프리셋으로 저장하거나 최근 이력에 남긴다.
3. 나중에 다시 선택해 즉시 재적용한다.

### 14-4. 저장 플로우
1. 사용자가 PNG 저장을 누른다.
2. 현재 캔버스가 저장된다.
3. 확장 버전에서는 CSV/Excel/PDF 저장도 제공한다.

---

## 15. 상태(State) 정의

| 상태ID | 상태명 | 설명 | 화면 반응 |
|---|---|---|---|
| ST-01 | 초기 상태 | 앱 첫 진입 | 라이트 모드 + 기본 계산값 표시 |
| ST-02 | 입력 변경 상태 | 사용자가 숫자 수정 중 | 실시간 재계산 |
| ST-03 | 정상 계산 상태 | 배치 결과 유효 | 결과/캔버스 정상 표시 |
| ST-04 | 경고 상태 | 주의가 필요한 조합 | 경고 배지/메시지 표시 |
| ST-05 | 계산 불가 상태 | 배치 불가 | 안내 메시지/빈 상태 표시 |
| ST-06 | 테마 전환 상태 | 화면모드 변경 | 전 UI 즉시 재스타일링 |
| ST-07 | 저장 완료 상태 | 저장 성공 | 토스트 또는 피드백 표시 |
| ST-08 | 수동 배치 상태 | 수동 모드 진입 | 좌표/가능 여부 표시 |

---

## 16. 반응형 정의

### 데스크톱
- 헤더 + 좌측 패널 + 우측 캔버스 2열 유지
- 추천 구조 전체 적용

### 태블릿
- 좌측 패널 폭 축소
- 캔버스 툴바 단순화
- 결과 카드 우선 노출

### 모바일
- 1열 구조
- 순서: 헤더 → 결과 요약 → 입력 → 가이드 → 캔버스 → 범례
- 캔버스는 접기/펼치기 또는 전체화면 버튼 제공 권장
- 화면모드 전환은 상단 우선 노출

---

## 17. 추가 보완사항 제안

### 17-1. 반드시 반영 권장
- 화면모드 3종과 라이트 기본값
- 입력 검증 메시지
- 결과 상태 배지
- 추천값 즉시 적용 버튼
- 캔버스 툴바
- 프리셋/최근 이력

### 17-2. 실무 활용 강화 제안
- 공정 옵션 요약 배지
- 비교 뷰(A/B)
- 메모 입력 칸
- 리포트 인쇄 화면
- 공유용 설정 JSON 저장

### 17-3. 접근성 제안
- 키보드 포커스 강조
- 색상 외 텍스트/아이콘 병행
- 충분한 명암 대비 확보
- 모바일 터치 영역 확대

---

## 18. v1 공식 추천 화면 구조 요약

### 헤더
- 앱명
- 화면모드 선택
- 도움말
- PNG 저장
- 확장 내보내기

### 좌측 패널
- 규격 설정
- 고급 공정 옵션
- 분석 결과
- 최적화 가이드
- 최근 이력 / 프리셋
- 범례

### 우측 메인
- 캔버스 툴바
- 메인 배치도
- 상태바

이 구조를 본 문서의 **공식 추천 화면 구조 v1**로 정의한다.

---

## 19. 다음 단계 연결 메모
다음 단계 문서에서는 아래를 이어서 구체화할 수 있다.
- `개발Stack 제안서.md`: 단일 HTML 개선형 / React 앱형 / 풀스택형 비교
- `프로토타입 프롬프트.md`: AI 코딩 도구용 구현 프롬프트 작성

